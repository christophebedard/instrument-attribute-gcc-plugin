name: Test
on:
  pull_request:
  push:
    branches:
      - master
  schedule:
    # Schedule twice a month, on the 1st and the 15th
    - cron: '0 0 1,15 * *'
jobs:
  gcc:
    strategy:
      matrix:
        include:
          - ubuntu: ubuntu-18.04
            gcc: gcc-7
            g++: g++-7
          - ubuntu: ubuntu-20.04
            gcc: gcc-9
            g++: g++-9
          - ubuntu: ubuntu-20.04
            gcc: gcc-10
            g++: g++-10
    runs-on: ${{ matrix.ubuntu }}
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: |
        sudo apt-get update && sudo apt-get install -q -y ${{ matrix.gcc }} ${{ matrix.g++ }} ${{ matrix.gcc }}-plugin-dev
        ${{ matrix.gcc }} --version
        ${{ matrix.g++ }} --version
    - name: Build
      run: |
        make TARGET_GCC=${{ matrix.gcc }} CXX=${{ matrix.g++ }} 
    - name: Test
      run: |
        make TARGET_GCC=${{ matrix.gcc }} CXX=${{ matrix.g++ }} verify
        make TARGET_GCC=${{ matrix.gcc }} CXX=${{ matrix.g++ }} test
